export const extensionversion = '1.1';

export const useFakeResults = true; // Set to true to generate our own results rather than asking the SE

// DEFAULT SETTINGS
// If there is nothing in storage, use these values.
//-------------------------
export var defaultsettings = {
  extensionswitch: 'on',
  apiserver: 'https://sourcesdeconfiance.org/api/trusted',
};
